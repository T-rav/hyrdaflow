extraction:
  python:
    python_setup:
      version: 3
    before_index:
      # CodeQL Python finalize currently references the old workspace path
      # (/home/runner/work/hyrda/hyrda) for this renamed repository.
      # Ensure that legacy path exists so finalize does not fail.
      - "mkdir -p /home/runner/work/hyrda/hyrda"
